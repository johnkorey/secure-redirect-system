# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=secure_redirect
DB_USER=postgres
DB_PASSWORD=postgres

# Application Configuration
PORT=3001
JWT_SECRET=your-secret-key-here
COMPANION_API_KEY=your-companion-app-secret-key-here

# IP2Location API (Optional - will use from system_configs if not set)
IP2LOCATION_API_KEY=

# Mailgun API (Optional - will use from system_configs if not set)
MAILGUN_API_KEY=
MAILGUN_DOMAIN=
MAILGUN_REGION=us

# Telegram Bot (Optional - will use from system_configs if not set)
TELEGRAM_BOT_TOKEN=
TELEGRAM_ADMIN_CHAT_ID=
